# Contributing

1. Install backend/frontend deps from their directories.
2. Never commit secrets, `.env`, `node_modules/`, or `.venv/`.
3. Run the test commands in README before opening a PR.
4. Update CHANGELOG for user-visible changes.
